import React from "react";
import { Outlet } from "react-router-dom";

const Auth = () => {
    const {location}
  return (
    <div>
      Auth
      <Outlet />
    </div>
  );
};

export default Auth;
